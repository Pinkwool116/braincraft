# 智能体架构说明

## 1. 智能体运作架构

早期版本采用“任务驱动 + 任务栈”的模式：模型根据环境和记忆不断生成任务并入栈，大任务再拆分为小任务。这个结构清晰，但在 Minecraft 这类动态环境中很快暴露出问题：环境变化频繁，任务执行中的修正也频繁，任务栈需要不断插入、删除、重排和回滚，管理成本会反过来消耗智能体的注意力。

当前架构改为更接近人类连续认知的 Agent Loop。智能体不再围绕固定任务栈运转，而是在循环中反复读取当前状态、记忆、计划文件、聊天记录和上一次工具结果，由 LLM 选择下一次工具调用；工具执行后的结果再写回上下文和工作记忆，进入下一轮循环。

整体由几层协同：

- Agent Loop Layer：主决策循环，负责构造提示词、调用决策模型、解析工具调用、执行工具、记录结果，并处理重复调用保护和聊天中断。
- Execution Layer：代码执行层。Agent Loop 给出自然语言步骤后，Coding LLM 生成 JavaScript，通过 IPC 发送给 Mineflayer Bridge 执行，并返回输出、错误和状态变化。
- Reflex Layer：反射层。独立于 Agent Loop 运行，处理战斗、低血量、着火、溺水、卡住、拾取物品、放火把等低层行为。
- Execution Coordinator：统一管理不同执行来源的优先级。生存反射优先级最高，可以中断中层任务；普通自动行为优先级较低，不会打断主要任务。
- Minecraft Bridge：连接 Python 大脑与 Mineflayer，负责状态上报、代码执行、聊天、技能调用、感知事件推送和主动扫描。

任务表达也从单一任务栈改为三个分层 Markdown 文件：

- `plan.md`：长期规划，记录阶段目标、背景约束、长期方向和延后事项。
- `todolist.md`：短期待办，支持嵌套、ID、状态切换、移动和整体重写，用来承载当前可执行的任务结构。
- `draft.md`：当前步骤草稿，记录给 Coding LLM 使用的技术思路、失败尝试、坐标、API 注意事项等。

这三个文件共同模拟人的认知分层：长期意图放在 `plan.md`，短期执行焦点放在 `todolist.md`，当前操作细节放在 `draft.md`。Agent Loop 每轮都会读取它们，因此任务结构可以自然演化，而不需要维护一个高成本的栈式控制器。

当前工具体系围绕这个循环展开：

- `execute_step`：把自然语言步骤交给执行层生成并执行代码。
- `chat`：向游戏内玩家发送消息，同时写入聊天记录。
- `recall_memory`：从长期记忆图谱中检索相关经验。
- `plan` / `todolist` / `draft`：分别修改长期规划、短期待办和当前步骤草稿。
- `scan_terrain`：主动触发宏观地形扫描和 LLM 地形分析。
- `inspect_surroundings`：按半径、目标和模式精确观察周围方块或实体。
- `wait`：主动等待，让环境继续变化并等待新的感知输入。
- `interrupt_execution`：中断正在执行的代码。

因此，智能体的核心不再是“先生成完整任务树再执行”，而是“持续感知、持续思考、持续选择工具、持续修正”。

## 2. 记忆架构

当前记忆系统采用“短期记忆 + 长期记忆图谱”的结构，由 `MemoryRouter` 统一对外提供接口。

短期记忆是工作记忆。它不是图，而是线性追加的经历缓冲，来源包括：

- Agent Loop 的思考和工具调用结果；
- 代码生成与执行记录；
- 聊天交互；
- 感知事件和地形观察；
- 反射层触发的生存事件。

工作记忆由原始日志和滚动摘要组成。原始条目持续写入 `working_memory_raw.json`，达到阈值后由 LLM 将“旧摘要 + 新条目”压缩成新的完整摘要，写入 `working_memory_summary.md`。这样既保留近期细节，又避免上下文无限膨胀。

长期记忆由 `GraphEngine` 维护为 NetworkX 有向图，并持久化到 `bots/{agent}/memory_graph`。节点类型包括：

- `event`：事件；
- `place`：地点；
- `person`：人物；
- `item`：物品、方块或实体；
- `time`：时间锚点；
- `pattern`：经验模式；
- `thought`：想法或反思；
- `community`：社区聚类生成的高级概念节点。

边用于表达空间、时间、因果、组成、社交、类属、关联和归纳关系，例如 `LOCATED_AT`、`HAPPENED_AT`、`LED_TO`、`INVOLVES`、`SUMMARIZED_FROM` 等。

短期记忆转长期记忆的过程称为 `crystallize`。它在任务边界或关键结构变化后触发：系统把工作记忆文本、相关旧图谱上下文和抽取规则交给 LLM，由 LLM 生成节点、边、失效标记和归纳结果；系统再将这些结果合并进图谱，并为新节点生成 embedding。

图谱更新不是简单追加。当前实现支持：

- 精确复用已有节点 ID；
- 按 `content + type` 去重；
- 合并 metadata；
- 标记节点或边 `invalid_at`，用于表示旧事实失效；
- 将多个低层事件归纳为 `pattern` 或其他摘要节点。

图谱维护由后台 Dream 机制完成。Dream 达到触发条件后异步运行，主要包括：

- 社区聚类：使用 Louvain 算法发现紧密子图，再由 LLM 生成 `community` 节点，并用 `CONTAINS` 连接代表节点。
- 去重：查找完全重复、编辑距离相近或 embedding 相似的节点，交给 LLM 决定合并。
- 剪枝：发现长期低访问节点和低权重老旧边，必要时删除或归纳。
- 归档：对长期失效且无人访问的节点做永久清理。
- 反思：把维护结果写成 `thought` 节点，形成对自身记忆状态的高层总结。

记忆检索采用“语义检索 + 精确匹配 + 扩散激活”。系统先用当前任务或查询文本寻找种子节点：embedding 匹配覆盖语义相似内容，子串匹配补足精确实体；随后从种子节点沿图谱边进行扩散激活，按边权、访问次数和近期访问加权，返回相关图谱切片。若已知两个实体，检索器还可以寻找它们之间的最短关系路径。

这个设计让智能体既能保留短期连续体验，又能把长期经验沉淀为可检索、可维护、可遗忘的结构化知识。

## 3. 感知架构

LLM 本身没有持续视觉和世界状态，因此当前架构专门补强了感知管线，让模型尽可能及时、低成本地获得环境变化。

感知由 JS 侧 `PerceptionWorker` 和 Python 侧 `PerceptionManager` 协同完成。

第一类是被动事件流。`PerceptionWorker` 持续监听 Mineflayer 事件，包括实体出现、实体靠近、实体消失、声音、天气变化、方块变化、物品拾取等。事件先在 JS 侧聚合，每 3 秒批量推送到 Python；Python 侧 `EventTicker` 每 2 秒消费缓冲区，用纯代码把原始事件压缩为简洁观察文本，并写入工作记忆。生命值和饥饿值变化也会被周期检测并记录。

第二类是周期地形扫描。JS 侧每 5 秒做快速射线扫描，用于紧急风险检测；每 30 秒做完整射线扫描；每 60 秒统计 8 格范围内的方块分布。完整扫描和方块统计通过独立通道发送给 Python，不与事件流混在一起。Python 侧 `TerrainAnalyzer` 每 30 秒在有新扫描数据时调用轻量 LLM，把射线样本和方块统计转化为自然语言地形理解，写入工作记忆。

第三类是紧急感知。`PerceptionWorker` 每 500 毫秒检查近距离敌对生物、熔岩、悬崖和溺水风险。一旦发现威胁，会绕过普通感知缓冲，直接推送给 Reflex Layer，由反射层立即战斗、后退、停止寻路或上浮。

第四类是主动观察工具。Agent Loop 可以根据当前任务主动调用：

- `scan_terrain`：触发新一轮完整扫描和地形分析，用于判断宏观地形、资源方向、风险和行动建议。
- `inspect_surroundings`：按半径精确检查周围方块与实体。小半径返回完整匹配信息，适合查脚边、头顶、建筑或挖掘坐标；大半径必须提供观察重点，并返回摘要。

因此，感知系统不是把全部世界状态一次性塞给模型，而是分层输入：高频事件用规则压缩，周期地形用 LLM 概括，危险信息直达反射层，复杂问题再由 Agent 主动观察。这样可以在上下文成本可控的前提下，让智能体持续获得足够新、足够关键的环境信息。
